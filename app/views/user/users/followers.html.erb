<h2>フォロワー</h2>
<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% if @user.present? %> 
    <% @user.each do |user| %>
      <tr>
        <td>
          <%= link_to user.name, user_path(user) %>
        </td>
        <td>  
          <%= user.introduction %>
        </td>
      </tr>
    <% end %>
  <% else %>
   <td>まだフォローをされていません。</td>
  <% end %>
  </tbody>
</table> 