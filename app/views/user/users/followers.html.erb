<h2>フォロワー</h2>
<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% if @users.present? %> 
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td>
          <%= link_to user.name, user_path(user) %>
        </td>
        <td>  
          <%= user.introduction %>
        </td>
      </tr>
    <% end %>
  <% else %>
   <td>まだフォローをされていません。</td>
  <% end %>
  </tbody>
</table> 